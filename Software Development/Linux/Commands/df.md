Show filesystem usage.  
```shell
df [path]
```
Options:  
- `-h`: Show sizes in a human-readable format

When `[path]` is provided, it shows the disk usage of the **filesystem that contains `[path]`**.
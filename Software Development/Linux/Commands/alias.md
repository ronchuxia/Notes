Check alias definition.  
```shell
alias <alias>
```

Set alias.  
```shell
alias <alias>='<command>'
```
Add it to shell config to make it permanent.

Common locations for alias definition:  
- `~/.bashrc`
- `~/.bash_aliases`: Often used by Ubuntu.

E.g.  
```shell
alias ll='ls -al'
```
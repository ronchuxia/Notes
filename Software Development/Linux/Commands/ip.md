Show all network interfaces on the machine and their IP addresses, MAC addresses, and interface state.  
```shell
ip address
```

NOTE: `ifconfig` is deprecated.

Show which route your machine would use to reach an IP address.  
```shell
ip route get <ip>
```